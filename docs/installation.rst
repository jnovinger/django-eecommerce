============
Installation
============

At the command line::

    $ easy_install django-eecommerce

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-eecommerce
    $ pip install django-eecommerce
